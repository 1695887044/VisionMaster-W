<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:VisionMaster.Scada.Controls">

    <!--
        矩形图元的外观。

        模板里两个元素叠放：形状画填充与边框，TextBlock 画标签。
        刻意不用 Border，而是用 Rectangle：Border 的 BorderThickness 是 Thickness 类型，
        把 double 的 StrokeThickness 绑上去需要一个转换器；而 Rectangle 的
        StrokeThickness/RadiusX/RadiusY 全是 double，TemplateBinding 可以直接对接，
        整条链上不需要任何 IValueConverter。

        Margin 用控件的 Padding：基类 ApplyStrokeInset() 已把边框线宽的一半（加呼吸空间）
        算进了 Padding，于是形状向内缩进后，视觉外沿正好落在 Width/Height 上。
    -->
    <Style TargetType="{x:Type local:RectangleElement}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:RectangleElement}">
                    <Grid>
                        <Rectangle
                            Margin="{TemplateBinding Padding}"
                            Fill="{TemplateBinding Fill}"
                            Stroke="{TemplateBinding Stroke}"
                            StrokeThickness="{TemplateBinding StrokeThickness}"
                            RadiusX="{TemplateBinding CornerRadius}"
                            RadiusY="{TemplateBinding CornerRadius}" />

                        <TextBlock
                            Margin="{TemplateBinding Padding}"
                            Text="{TemplateBinding Text}"
                            Foreground="{TemplateBinding Foreground}"
                            FontFamily="{TemplateBinding FontFamily}"
                            FontSize="{TemplateBinding FontSize}"
                            FontWeight="{TemplateBinding FontWeight}"
                            TextAlignment="Center"
                            TextWrapping="Wrap"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
