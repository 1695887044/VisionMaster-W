# Tasks

- [x] Task 1: 创建解决方案骨架与依赖
  - [x] SubTask 1.1: 新建 `E:\VM\FlyingShearSimulator` 解决方案，含 `FlyingShear.Core`(net48 类库)、`FlyingShear.Simulator`(net48 类库)、`FlyingShear.App`(net48 WPF) 三个项目并互相引用（App→Simulator→Core，App→Core）
  - [x] SubTask 1.2: 安装 NuGet：Core 无依赖；Simulator 引 NModbus；App 引 HelixToolkit.Wpf.SharpDX、CommunityToolkit.Mvvm、NModbus（附 nuget.config 使用 nuget.org v3 源）
  - [x] SubTask 1.3: App 中还原 HelixToolkit 视口空白场景，确认编译通过、窗口可显示 3D 背景
- [x] Task 2: Core 层——寄存器映射与数据模型
  - [x] SubTask 2.1: 定义 `RegisterMap` 常量类（全局 0~9、追剪 10~19、飞剪 20~29、工位3 30~39、凸轮表 40~44，UInt32/Int32 高字在前约定）
  - [x] SubTask 2.2: 定义强类型模型：`LineState`（主轴）、`TrackingShearState`（工位1）、`RotaryShearState`（工位2）、`PhaseStationState`（工位3）、`CamTableState`，以及 UInt16 数组 ↔ 模型双向转换（StateCodec）
- [x] Task 3: Core 层——虚拟多轴运动学引擎
  - [x] SubTask 3.1: 实现 `VirtualAxis`（位置/速度/加速度）、`MasterAxis`（设定速度、累计行程计米器，VirtualPlcEngine 内实现）
  - [x] SubTask 3.2: 实现 `CamTable`：主轴→从轴归一化点表 + 线性插值/Slope；内置追剪表 ×2、飞剪三区段表（等速/加速/减速区，CamTableLibrary 动态生成）
  - [x] SubTask 3.3: 实现耦合状态机：自由/啮合中(Ramp)/耦合中/脱离中(Ramp)；电子齿轮模式（比×100 寄存器）；相位偏移斜坡追赶（在线修改不跳变）
- [x] Task 4: Core 层——三工位工艺逻辑与验证
  - [x] SubTask 4.1: 工位1 追剪状态机：计米器严格节拍触发（漏切=工艺约束教学点）→啮合→同步窗口内速度=材料速度→剪切(计数+1)→脱离返回；窗口起点/长度可写
  - [x] SubTask 4.2: 工位2 飞剪状态机：切长 vs 周长自动选择等速/加速/减速区段；刀刃相位偏移在线调整；0.01° 角度分辨率
  - [x] SubTask 4.3: 工位3 相位冲压：齿轮耦合+相位偏移+冲压间距触发计数；凸轮表在线切换命令处理
  - [x] SubTask 4.4: 编写控制台验证脚本（tools/SimTest）：循环间隔=切长 ✓、齿轮比 2:1 ✓、相位追赶无跳变 ✓、飞剪区段 ✓、编解码往返 ✓ —— ALL PASSED
- [x] Task 5: Simulator 层——Modbus TCP 从站
  - [x] SubTask 5.1: NModbus 从站监听 127.0.0.1:502；20ms 周期把运动学引擎输出刷入寄存器 0~99；接收主站写入（参数/命令字）回灌引擎输入（PointSource ReadPoints/WritePoints 桥接）
- [x] Task 6: App 层——Modbus 主站通信服务
  - [x] SubTask 6.1: 50ms 轮询寄存器 0~44 → 强类型状态；FC06/FC16 写参数与命令；UInt32/Int32 字序处理；后台 Thread + Dispatcher 投递
  - [x] SubTask 6.2: 断线自动重连（指数退避至 10s 上限）与连接状态事件
- [x] Task 7: App 层——ViewModel
  - [x] SubTask 7.1: `MainViewModel`（CommunityToolkit.Mvvm）：连接管理、模拟器启停、主轴控制（速度/启动/停止/复位）、三工位参数命令与状态仪表属性、凸轮表选择与在线切换、故障告警；订阅通信服务更新
- [x] Task 8: App 层——3D 场景程序化建模
  - [x] SubTask 8.1: 全线布局：底座+导轨、材料长条（移动色标表现速度与冲压间距）
  - [x] SubTask 8.2: 工位1 直线剪臂组合体、工位2 旋转刀辊（圆柱+刀刃+键槽标记）、工位3 冲压头、同步窗口半透明色块（参数变化重建几何）
  - [x] SubTask 8.3: 相机预设（整线全景/工位聚焦）与鼠标交互、正交切换
- [x] Task 9: App 层——3D 数据驱动动画
  - [x] SubTask 9.1: 剪臂 X 向移动+剪切下压；刀辊旋转+剪切闪光；冲压头下压+相位滑移；材料色标随行程滚动；CompositionTarget.Rendering 平滑插值（渲染与轮询解耦）
- [x] Task 10: App 层——凸轮曲线 2D 图表
  - [x] SubTask 10.1: 自制 OnRender 图表控件：凸轮表曲线+实时工作点高亮+引线，切换表号曲线更新
- [x] Task 11: App 层——主界面布局
  - [x] SubTask 11.1: MainWindow 布局：左侧连接/模拟器/主轴控制、右侧三工位面板+凸轮表面板、中央 3D 视图；全 MVVM 绑定，状态中文+颜色，故障红色告警，底部教学点提示
- [x] Task 12: 端到端联调验证
  - [x] SubTask 12.1: 解决方案编译 0 警告 0 错误；App 启动 14s+ 无崩溃（3D 视口/灯光/场景初始化正常）
  - [x] SubTask 12.2: SimTest modbus 模式：从站→主站写速度/参数/启动命令→计米器 61→5342mm、追剪计数 0→2、切长参数回读一致 → PASS
  - [x] SubTask 12.3: 运动学验证：循环间隔=切长(2000mm)、齿轮比 2:1 速度倍增、相位追赶无跳变、短切→加速区/长切→减速区 → ALL PASSED（凸轮表在线切换曲线更新与 3D 视觉效果待用户目视确认）

# Task Dependencies
- Task 2、3 依赖 Task 1；Task 3 内部 3.1→3.2→3.3 顺序
- Task 4 依赖 Task 3（工艺逻辑复用轴引擎）
- Task 5 依赖 Task 4（从站刷入需要完整工艺逻辑）
- Task 6 依赖 Task 1、2（需 NModbus 与数据模型）；与 Task 3/4 可并行
- Task 7 依赖 Task 6、2
- Task 8 依赖 Task 1；与 Task 6/7 可并行
- Task 9 依赖 Task 8、7；Task 10 依赖 Task 7
- Task 11 依赖 Task 7、8、10
- Task 12 依赖 Task 5、9、10、11
- 已按 [Core+Simulator] / [通信+VM] / [3D] 三条线推进并汇合验证
