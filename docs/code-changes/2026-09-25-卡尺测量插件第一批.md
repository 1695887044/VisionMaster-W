# 2026-09-25 卡尺测量插件 第一批（Plugin.CaliperMeasure）

> **第二批已交付**（同日）：点线距/圆直径/角度三种拟合类测量 + 拟合参数区，全文见文末「第二批」章节。

## 1. 需求与决策

### 1.1 需求来源

这是「补齐视觉核心工具链」的又一步：仓库已有图像采集、预处理、ROI、脚本、Blob 缺陷检测、OCR、读码等能力，
但**缺少最基础也最常用的尺寸测量能力**。用户选定「测量」方向，规格在开工前已逐条确认完毕，
本次任务只负责按规格实现**第一批**，不改变设计。

### 1.2 已确认的设计决策（照录规格，不得更改）

| # | 决策项 | 结论 |
| --- | --- | --- |
| 1 | **交付范围** | 完整形态要支持 5 种测量（宽度/间隙、两点距、点线距、圆直径、角度），**分两批**；本批只做前两种（均不涉及拟合） |
| 2 | **工程/命名** | 工程名 `Plugin.CaliperMeasure`，插件名 `卡尺测量`，分组 `测量` |
| 3 | **卡尺建模** | **阵列式**：用户不逐个摆卡尺，而是给一个"搜索区"形状 + 卡尺数 N，插件内部自动均布 N 个卡尺 |
| 4 | **输出端口 6 个** | `MeasureValue`(double) / `Deviation`(double) / `IsOk`(bool) / `MeasureImage`(HImage) / `FittedGeometry`(HXLD) / `EdgePoints`(HXLD) |
| 5 | **判定规格** | 标准值 + 上公差 + 下公差（上下分开填，支持非对称公差，如 +0.05/-0.02） |
| 6 | **像素当量** | 插件内自带参数（mm/pixel，默认 1.0），输出物理值；项目目前没有标定模块，将来有了可改成连线取值 |
| 7 | **一个步骤测一个尺寸** | 要测多个尺寸就放多个步骤 |

**为什么分两批**：后三种（点线距/圆直径/角度）都要先做**几何拟合**（`fit_line_contour_xld` / `fit_circle_contour_xld` /
角度计算），与"直接找边取距离"是两类算法；且拟合还引入一组拟合参数（迭代数、ClippingFactor 等），
界面上要新增"拟合参数"分区。一次性交付会把风险摊薄、验证量翻倍。因此第一批先把"阵列卡尺 + 找边 + 判定 + 标注"
这条主干打通并实测，第二批在同一骨架上加拟合分支即可（枚举加成员、界面下拉自动多两项，XAML 结构不动）。

### 1.3 参数分区（5 区）

① 测量类型　② 搜索区与卡尺（画布矩形 + 卡尺数 N）　③ 找边参数（Sigma / 边缘阈值 / 极性 / 选择 / 插值）
④ 拟合参数（**本批不涉及拟合，界面不显示；不写用不到的空壳参数**）　⑤ 判定与标定（标准值 / 上公差 / 下公差 / 像素当量）

### 1.4 默认值（通用起点，逐条注明依据；**绝不针对任何样图调**）

| 参数 | 默认 | 依据 |
| --- | --- | --- |
| 卡尺数 N | 5 | 工业常用起步值，兼顾抗噪（多点平均）与速度 |
| Sigma | 1.0 | HALCON 测量例程常用的轻度平滑 |
| 边缘阈值 | 30 | HALCON 测量例程（含仓库脚本模板库）的常用对比度门限 |
| 极性 | 全部 | 不预设明暗方向，最宽容的起点 |
| 选择 | 全部 | 不预设取哪条边 |
| 插值 | bilinear | 亚像素精度与速度的通用折中 |
| 像素当量 | 1.0 | 等于直接输出像素值，不假设任何标定 |
| 标准值/上公差/下公差 | 0 / 0 / 0 | **有意的零容忍**：通用平台算子不该假装知道产品规格；用户看到默认即 NG 会立刻去填规格。与 BlobDetect 的 `MaxDefectCount=0` 同一风格（安全侧默认，宁严勿松） |

### 1.5 本次实现中查证/发现的关键事实（决定了算法怎么写）

1. **扫描方向 = L1(Phi) 轴**（用真实 HALCON 实证，见 3.4 探针）：`gen_measure_rectangle2` 沿矩形**主轴 L1** 扫描，
   检出的边缘**垂直于** L1 轴；`Phi=0` 时 L1 轴为水平方向。
   - 由此推出：**N 把卡尺必须沿"垂直于 Phi"的方向（L2 轴）均布**，每把保留完整 L1 扫描长度。
   - 插件目录里 `Models\CaliperRegion.cs` 原有的几何注释把扫描/均布方向**写反了**，本次已修正并在注释里记录实证过程。
2. **画布形状参数与测量算子参数顺序一致**（规格点名的基石）：`DrawShapeType.Rectangle` 的参数是
   `(中心行 R, 中心列 C, 角度 Phi, 半长 L1, 半宽 L2)`，与 `gen_measure_rectangle2` 完全一致，
   **无需换算层**，搜索区参数可直接喂给算子。
3. **HALCON .NET 是引用计数语义**（用真实 HALCON 实证）：
   - `new HImage(obj)` / `new HXLDCont(obj)` 是**独立句柄**（源对象释放后包装对象仍可用，非所有权转移）；
   - 但**只释放源对象、不释放包装对象会泄漏**：500 次不释放包装泄漏约 160MB。
   - 推论：交给端口的包装对象由基类回收，中间"源对象"必须进 `temp` 释放——两者都要释放，缺一不可。
   - 顺带发现：`dump_window_image` 回读出的 `shot` 也是源对象，包装后必须显式释放（本插件已做；BlobDetect 未做，属其既有问题，本次未改它）。
4. **Sigma 有下限**：`measure_pairs` 传 `Sigma=0` 会抛 `#1302 Wrong value of control parameter`。
   本插件把 Sigma 夹取下限设为 0.4（HALCON 要求），界面校验也改为 `≥0.4`。
5. **搜索区中心在图外时**，算子抛 `#3022 Wrong size of filter for Gauss`——英文错误对操作员无指导意义，
   本插件改为**提前用中文报错**（见 1.6）。

### 1.6 规格之外的实现决策（诚实标注：这些是我做的工程判断，非规格明文）

| 决策 | 内容 | 理由 |
| --- | --- | --- |
| **均布规则** | 把搜索区整段 L2 宽度等分成 N 份，每把卡尺半宽 = L2/N（正好铺满、不重叠） | "均布 N 个卡尺填满搜索区"的自然解释；每把取独立的一段做灰度平均，互不干扰 |
| **必须全部检出** | 只要有 1 把卡尺找不到边，就判**失败**（而非跳过取平均） | 把检到的几把平均出一个"看似合理"的数是计量最危险的失败模式（错误但不出错）；宁可报错让用户调参数 |
| **宽度取值口径** | 每把卡尺取**第一条配对**的 `IntraDistance`，再对 N 把取平均 | 多条成对边缘时取扫描方向第一条；跨卡尺平均是抗噪主手段 |
| **两点距取值口径** | 每个搜索区：每把卡尺 `measure_pos` 取**第一条边点**，N 把的点取**平均**成该区"一个代表点"，再 `distance_pp` | 规格要求"各出一个边点"；平均是"多点抗噪"下的稳健代表点。`Select=最后一条` 时 `measure_pos` 本就只返回最后一条 |
| **越界提前报错** | 搜索区中心超出图像范围 → 中文 Fail（指出哪个区、中心坐标、图尺寸） | 替代算子那条 #3022 英文错误 |
| **默认搜索区播种** | 首次打开视图/换图时，若搜索区不足则按类型补默认区（宽度=1、两点距=2），把图按所需数量切成等宽竖条，每区取本条中间、覆盖 35%×25%，Phi=0 | 给一个"能立刻拖动"的通用起手姿态，不针对任何样图；只增不减，绝不删用户已摆好的区域 |
| **标注底图** | 用**原图**（彩色输入保留彩色） | 与 BlobDetect 同口径，标注图是给人看的 |

---

## 2. 修改文件清单

新增目录 `Plugins\Plugin.CaliperMeasure\`（9 个文件）+ 修改 `VisionMaster.sln`。

### 2.1 `Plugins\Plugin.CaliperMeasure\Plugin.CaliperMeasure.csproj`

`net9.0-windows` + `UseWPF` + `Nullable enable` + `ImplicitUsings`，ProjectReference 引
`Core.Halcon` / `Core.Controls` / `Core.Interfaces` / `UI`，Reference 引 `halcondotnet`（与 `Plugin.BlobDetect.csproj` 同构）。
编译后由 `Plugins\Directory.Build.targets` 的 `CopyPluginToModules` 自动投递到仓库根 `Modules\`，**无需任何注册代码**。

### 2.2 `Plugins\Plugin.CaliperMeasure\CaliperEnums.cs`

`MeasureKind`（宽度/间隙、两点距）、`EdgePolarity`（暗→亮 / 亮→暗 / 全部）、`EdgeSelect`（第一条 / 最后一条 / 全部）、
`EdgeInterpolation`（nearest / bilinear / bicubic）、`StatusLevel`（Info/Warning/Error）。枚举成员带 `[Display(Name)]`。
**后三种测量类型故意暂不放进枚举**：放了就是"选了也没结果"的死选项；第二批补齐算法后再加成员，界面下拉自动多两项。

### 2.3 `Plugins\Plugin.CaliperMeasure\Models\CaliperRegion.cs`

一个搜索区（可序列化）。`Params` 与 `GenRectangle2` 一一对应 `[中心行, 中心列, 角度Phi, 半长L1, 半宽L2]`；
`ParamEntries` 提供数值框微调行；`ParamEdited` 事件把"数值框改值"回传 ViewModel 去同步画布。
与 `Plugin.CreateRoi.Models.RoiItem` 同一套写法。本次修正了类注释里**写反的扫描/均布几何语义**（见 1.5）。

### 2.4 `Plugins\Plugin.CaliperMeasure\NumberValidationRule.cs` / `EnumDisplayNameConverter.cs`

数值输入的中文校验规则（解析 + 区间 + 整数约束，失败红框 + 中文提示，非法值进不了 ViewModel）；
枚举 → 中文名转换器（读 `[Display(Name)]`，避免把中文硬写在 XAML 里变成第二份真相）。

### 2.5 `Plugins\Plugin.CaliperMeasure\AnnotationRenderer.cs`

离屏 `buffer` 窗口合成标注图（照搬 BlobDetect 已验证的做法）。本次新增**测量连线**图层，并修了两处：

```csharp
// 画布大小取自底图；尺寸变了 EnsureWindow 会自动重建窗口（HALCON 反复 open/close 有 #9302 死锁风险）
HOperatorSet.OpenWindow(0, 0, w, h, "white", "buffer", "local", out HTuple win);

HOperatorSet.DispObj(baseImage, win);                 // 底图
// 卡尺小矩形（黄，1px）→ 测量连线（青，2px）→ 边缘点十字（绿，2px）→ 左上角文字
// 关键：只给 'box_color' 不会画框，必须同时给 'box','true' 才真正画出白底方框
HOperatorSet.DispText(win, new HTuple(lines), "window", 12, 12, textColor,
    new HTuple("box", "box_color"), new HTuple("true", "white"));

// 引用计数语义：包装后必须释放源对象 shot，否则每次渲染漏一个引用计数（实测 500 次泄漏约 160MB）
HOperatorSet.DumpWindowImage(out HObject shot, win);
var result = new HImage(shot);
shot.Dispose();
return result;
```

### 2.6 `Plugins\Plugin.CaliperMeasure\CaliperMeasurePlugin.cs`（主体）

- 元数据：`[Display(Name="卡尺测量", GroupName="测量", ShortName="\uf545", Description=...)]`
- 端口：`SrcImage` 输入 + 6 个输出（名字/类型严格按规格）
- 参数：10 个 `[StepConfig]` 属性（测量类型、卡尺数、Sigma、边缘阈值、极性、选择、插值、标准值、上公差、下公差、像素当量），
  每个 setter 都 `SchedulePreview()`
- 画布同步：与 CreateRoi 同一范式（`CanvasRegions` ↔ `CaliperRegions`、`CanvasActiveRegion` ↔ `SelectedRegion`，
  拖拽写 `info.HTuples`、数值框写回 `info.HTuples`，双向同步）
- 预览：`OnViewLoaded` 补搜索区取图、`DispatcherTimer` 200ms 防抖、`UiDispatcher` 跨线程投递、
  `RefreshPreview` 在 UI 线程同步跑，并订阅 `SrcImage.ValueChanged`
- 算法核心与流程运行共用一份 `ExecuteMeasureCore`

**关键片段（a）画布形状 → 测量算子 参数直通 + 阵列均布**：

```csharp
/// 扫描方向 = L1(Phi) 轴（已实证）；所以 N 把卡尺沿"垂直于 Phi"的 L2 轴均布，每把保留完整 L1 扫描长度
private static List<double[]> DistributeCalipers(double[] rect, int n)
{
    double r = rect[0], c = rect[1], phi = rect[2], l1 = rect[3], l2 = rect[4];
    if (n <= 1) return new List<double[]> { new[] { r, c, phi, l1, l2 } };

    double spacing = 2.0 * l2 / n;                       // 整段 L2 宽度等分 N 份
    double halfWidth = Math.Max(spacing / 2.0, MinCaliperHalfWidth);
    double cosPhi = Math.Cos(phi), sinPhi = Math.Sin(phi);
    var list = new List<double[]>(n);
    for (int i = 0; i < n; i++)
    {
        double t = -l2 + spacing * (i + 0.5);            // 沿 L2 轴的偏移量
        list.Add(new[] { r + t * cosPhi, c - t * sinPhi, phi, l1, halfWidth });  // L2 轴单位向量=(cosPhi,-sinPhi)
    }
    return list;
}

// 搜索区参数顺序与 gen_measure_rectangle2 完全一致，直接喂进去（无需换算层）
HOperatorSet.GenMeasureRectangle2(c[0], c[1], c[2], c[3], c[4], imgW, imgH, p.Interpolation, out HTuple handle);
measureHandles.Add(handle);
```

**关键片段（b）两种测量的取值**：

```csharp
// 宽度/间隙：measure_pairs 直接给成对边缘间距 IntraDistance
HOperatorSet.MeasurePairs(gray, handle, p.Sigma, p.EdgeThreshold, p.Transition, p.Select,
    out HTuple r1, out HTuple c1, out _, out HTuple r2, out HTuple c2, out _, out HTuple intra, out _);
if (intra.Length == 0) { /* 只要一把找不到边就 Fail，绝不用"部分平均"糊弄过去 */ }
widths.Add(intra[0].D);

// 两点距：每区 measure_pos 取第一条边点，N 把取平均成一个代表点 → distance_pp
HOperatorSet.MeasurePos(gray, handle, p.Sigma, p.EdgeThreshold, p.Transition, p.Select,
    out HTuple rowEdge, out HTuple colEdge, out _, out _);
HOperatorSet.DistancePp(p1.R, p1.C, p2.R, p2.C, out HTuple dist);
```

**关键片段（c）测量句柄释放（严格 finally）**：

```csharp
var temp = new List<HObject>();           // 中间 HALCON 对象
var measureHandles = new List<HTuple>();  // gen_measure_rectangle2 建的测量对象
try { /* ... 算法 ... */ }
finally
{
    foreach (var h in measureHandles) { try { HOperatorSet.CloseMeasure(h); } catch { } }  // 算子名是 close_measure
    foreach (var o in temp) { try { o.Dispose(); } catch { } }
}
```

**关键片段（d）`RunAlgorithm` 开轮重置 + NG 不写 Fail**：

```csharp
MeasureValue.Value = 0; Deviation.Value = 0; IsOk.Value = false;  // 基类只回收 IDisposable，这三个要手动清
// ... 执行 ...
if (!result.IsOk) ErrorMessage.Value = result.NgReason;           // NG 是正常结果，Success 保持 true
```

### 2.7 `Plugins\Plugin.CaliperMeasure\CaliperMeasureView.xaml` / `.xaml.cs`

左列参数五区（卡片 + 可滚动）+ 右列 `h:ImageEdit`（既是标注图预览，又是搜索区拖拽画布）+ 底部信息栏（颜色随 `StatusLevel`）。
配色/圆角/字体与 `ImageAcquisitionView`、`CreateRoiView`、`BlobDetectView` 同一套；数值框统一 `NumericBox` 样式。
代码后置只在 `Loaded` 时通知 VM（与既有插件同构）。

### 2.8 `VisionMaster.sln`（修改）

新增 `Plugin.CaliperMeasure` 工程条目（GUID `{C1A7E4D9-3F62-4B08-9E51-7A2D6C4F8B03}`）+ 6 组配置映射 +
归入 `Plugins` 解决方案文件夹。

---

## 3. 验证结果

> 全部命令的真实输出。工作区长期有并行任务在改 Core/Engine/其他插件，外部状态在两次命令间发生过变化，如实记录。

### 3.1 单工程编译（验收项 1）

```
dotnet build Plugins\Plugin.CaliperMeasure\Plugin.CaliperMeasure.csproj -v m --no-dependencies -clp:ErrorsOnly
→ 已成功生成。  0 个警告  0 个错误
```

**0 个错误、0 个警告。**（不带 `--no-dependencies` 的完整依赖编译同样是 0 错误 0 警告。）

### 3.2 解决方案级编译（验收项 2）

- 第一次 `dotnet build VisionMaster.sln -v m -clp:ErrorsOnly`：**1 个错误，来自并行任务（YOLO 插件线）**：

  ```
  Plugins\Plugin.Yolo\YoloPlugin.cs(250,24): error CS0246: 未能找到类型或命名空间名"YoloView"
  ```

  该错误**与本次改动无关**（`Plugins/Plugin.Yolo/` 是并行任务的未跟踪新目录，处于中间态）。
  按规程**未尝试修复**，改用 `--no-dependencies` 单独验证本工程（见 3.1，通过）。

- 紧接着复核时再跑一次完整解决方案构建：

  ```
  dotnet build VisionMaster.sln -v m -clp:ErrorsOnly
  → 已成功生成。  1 个警告  0 个错误
  ```

  说明：并行任务在此期间补齐了 YOLO 插件，那条外部错误已消失；**本次改动没有引入任何错误**，
  新工程确实被解决方案纳入并成功编译。

### 3.3 产物投递（验收项 3）

```
ARTIFACT: 2026/09/25 13:46:33  58880 bytes
E:\VM\VisionMaster-W-master\Modules\Plugin.CaliperMeasure.dll
```

由 `Plugins\Directory.Build.targets` 的 `CopyPluginToModules` 自动投递，宿主扫描 `Modules\Plugin*.dll` 加载。

### 3.4 算法正确性实测（验收项 5，仓库外临时控制台）

在**仓库外**建临时控制台 `C:\Temp\CaliperVerify`（net9.0-windows + WPF，**引用构建出的 `Plugin.CaliperMeasure.dll`**），
用合成图真跑，**验证后已整目录删除、仓库内无残留**。结果：**38 项断言全部通过**。关键输出：

```
[PORTS] inputs=[SrcImage]
[PORTS] outputs=[MeasureValue,Deviation,IsOk,MeasureImage,FittedGeometry,EdgePoints,Success,ErrorMessage]
---- 2. 宽度/间隙（真值 40 px）----
[OUT] MeasureValue=40 Deviation=40 IsOk=False
[OUT] MeasureImage.init=True Fitted.init=True Edge.init=True
  [√] 宽度：测量值 = 40 px（亚像素误差 < 0.2）
  [√] 宽度：边缘点为 5×2=10 个十字        [√] 宽度：测量连线为 5 条
[ANNO] 400x400 channels=3
[ANNO-TEXT] 左上角灰度 max: 源=0 标注=255    → 白底框确实画上了
---- 3. 像素当量 / 判定 / 偏差符号 ----
[PX=1.0] MeasureValue=40        [√] 当量 1.0：物理值 = 像素值（≈40）
[PX=0.1] MeasureValue=4         [√] 当量 0.1：物理值 = 像素值 × 0.1（≈4.0）
[JUDGE-OK]   Deviation=0    IsOk=True   [√] 偏差在公差内 → OK
[JUDGE-UP]   Deviation=+1   IsOk=False  Err='偏差 +1 超出上公差（上公差 0.05）'   [√] 测大为正 / [√] 写明上公差
[JUDGE-DOWN] Deviation=-1   IsOk=False  Err='偏差 -1 超出下公差（下公差 -0.02）'   [√] 测小为负 / [√] 写明下公差
[ASYM] 标准=4.03 Deviation=-0.03 IsOk=False  [√] 非对称公差方向正确（-0.03 < -0.02 → NG）
---- 4. 两点距（真值 220 px）----
[OUT] MeasureValue=220 Deviation=220 IsOk=False
  [√] 两点距：距离 = 220 px（误差 < 0.3）  [√] 测量连线 1 条  [√] 边缘点 5+5=10 个
---- 5. 资源不泄漏（40 轮，分批观测）----
[MEM] 第 1 批(10 轮)：本批增量=504KB
[MEM] 第 2 批(10 轮)：本批增量=-1372KB
[MEM] 第 3 批(10 轮)：本批增量=8KB
[MEM] 第 4 批(10 轮)：本批增量=0KB        → 预热后收敛，无逐轮线性泄漏
---- 6. 边界（不崩 + 中文说明）----
[EMPTY]           Err='输入图像为空或未初始化'（且 Value=0 IsOk=false，开轮重置有效）
[LESS-REGION]     Err='第 2 个搜索区的第 1 把卡尺未检出边缘（共 5 把）：请降低边缘阈值、放宽极性，或调整/放大搜索区'
[OUT-OF-BOUNDS]   Err='搜索区「搜索区1」中心 (5000, 5000) 超出图像范围（400×400）：请把搜索区拖到图像内'
[HIGH-THRESHOLD]  Err='第 1 把卡尺未检出成对边缘（共 5 把）：请降低边缘阈值、放宽极性，或调整/放大搜索区'
[NOT-RECT]        Err='搜索区「搜索区1」不是矩形：卡尺测量只支持矩形搜索区（请删除该形状后新建矩形）'
[DEGENERATE]      Err='搜索区「搜索区1」尺寸过小（半长 L1=0、半宽 L2=80，两者都需 ≥ 1）：请拖大搜索区…'
[CLAMP]           Execute=True（Sigma=-5 / 阈值=0 / 卡尺数=0 被静默夹取，不抛异常）
---- 7. 视图 XAML 加载 + 布局 ----
[VIEW] CaliperMeasureView 加载+布局+渲染 OK；非空像素=270448
```

**逐项对照验收标准**：

1. **宽度测量**：竖直亮带真值 40 px → 实测 **40.0**（亚像素误差 < 0.2）。✓
2. **两点距**：两条亮带左边缘真值 220 px → 实测 **220.0**。✓
3. **像素当量生效**：当量 1.0 → 40.0（= 像素值）；当量 0.1 → 4.0（= 像素值 × 0.1）。✓
4. **判定逻辑**：标准值/公差调整后 `IsOk` 正确翻转；偏差符号正确（测大 +1、测小 -1）；非对称公差（+0.05/-0.02）方向正确。✓
5. **资源不泄漏**：40 轮分批观测，预热后每批增量收敛到 8KB / 0KB，**无逐轮线性增长**（若测量句柄或包装对象泄漏，每轮会留 ~330KB 量级）。✓
6. **边界**：空输入、搜索区越界、无边缘可检（阈值过高）、非矩形、退化矩形、非法参数，**全部不崩且给中文说明**。✓
7. **视图 XAML 能加载**：实例化 `CaliperMeasureView` + Measure/Arrange + `RenderTargetBitmap` 渲染，非空像素 270448。✓

**目视复核标注图**（导出的 `anno.png`，已随临时目录删除）：5 个黄色卡尺沿垂直方向阵列均布、各横跨白条；
绿色十字正落在左右边缘（10 个）；青色测量连线连接成对边缘；左上角白底红字「判定：NG / 测量值：40 mm / 偏差：+40 mm」。

### 3.5 冒烟回归（验收项 4）

```
dotnet run --project FlowCanvasChecks\FlowCanvasChecks.csproj -c Debug --no-build
→ 通过: 610  失败: 14
```

- 任务书给的基线是「583 通过 / 1 失败」，本次跑到 **610 通过 / 14 失败**：通过数上升、失败数也上升，
  **全部属于并行任务**（`FlowCanvasChecks` 被并行任务新增了 OCR / 颜色检查 / 线序检测等检查）。
- **决定性证据**：整份冒烟输出里搜索 `Caliper|卡尺` **零匹配**（匹配数 0），
  即冒烟套件根本不覆盖本插件，14 项失败与本插件无关（逐条核对均属"线序检测"配方线）。

> 说明：`FlowCanvasChecks` 工程 ProjectReference 了并行任务的 `Plugin.Yolo`，故本次用 `--no-build`
> 跑已有的测试产物，避免被并行任务的中间态阻塞。

---

## 4. 已知边界

1. **本批只支持 宽度/间隙 与 两点距**：点线距、圆直径、角度三种**未实现**（枚举里也没有成员，避免死选项）。
   第二批补齐拟合算法后加入枚举即可。
2. **像素当量是手填常量**：默认 1.0，无标定模块联动（项目暂无标定模块）。当量=1.0 时数值上等于像素值，
   但界面与标注仍按参数语义显示单位 `mm`——**未标定时把"像素数"当 mm 看是使用者的责任**，建议在方案里把当量填成实测值。
3. **未做斜边/曲面补偿**：卡尺是直线扫描，被测边若明显倾斜、曲面或存在锥度，测量的是"扫描线方向的投影距离"，
   需要用户把搜索区摆正或改用拟合类算法（第二批）。
4. **标注图渲染依赖离屏窗口**：`open_window('buffer')` 在无显示环境（纯服务/无桌面会话）下理论上有失败可能；
   一旦失败只丢 `MeasureImage`（记一条 Warn），测量结果（值/偏差/判定/几何 XLD）不受影响。
5. **"必须全部卡尺检出"是刻意从严**：任一把卡尺找不到边即判失败（不用"部分平均"）。副作用是搜索区里
   只要有一小段落在无特征区就会失败——这是计量安全的取舍，现场需把搜索区摆到都有边的地方。
6. **均布规则固定为"等分铺满 L2、每把半宽=L2/N"**：规格只说了"均布 N 个卡尺"，具体铺法由本插件定。
   若以后想改成"等间距中心 + 固定半宽（允许重叠）"，只需改 `DistributeCalipers` 一处。
7. **两点距的"一个代表点"取 N 把卡尺边点的平均**：`Select=全部` 时每把取第一条边。若被测边本身弯曲，
   平均点会落在中点附近，与"取某一把的点"口径不同——已在 1.6 记录。
8. **`FittedGeometry` 本批承载的是"测量连线"**（宽度=各卡尺两边缘连线、两点距=两点连线），不是拟合几何；
   第二批的点线距/圆直径/角度会在这里放拟合出的直线/圆/角度几何（端口名与类型不变）。
9. **未纳入自动冒烟**：`FlowCanvasChecks` 不覆盖本插件（冒烟输出里 `Caliper/卡尺` 零匹配）。
   本次算法验证靠"仓库外临时控制台真跑 + 目视标注图"完成，该控制台已删除、不可复现；
   后续若要长期守护，建议把「40px 亮带测出 40.0 / 220px 两点距测出 220.0」固化成正式断言。
10. **`dump_window_image` 源对象必须显式释放**：这是本次实测发现的 HALCON .NET 引用计数陷阱
    （不释放会每轮泄漏）。本插件已规避；同仓库 `Plugin.BlobDetect` 存在同样写法（未释放 `shot`），
    按"不改动其他插件"的约束本次未动它，**建议后续单独排查**。

---

## 5. 学到的知识点（本次）

1. **HALCON 测量的几何语义必须实证**：`gen_measure_rectangle2` 沿 **L1(Phi) 轴扫描**，边缘垂直于 L1 轴；
   均布阵列要沿**垂直方向（L2 轴）**排。不要凭"半长/半宽"的字面猜，用一张合成图 3 分钟就能定死。
2. **HALCON .NET 是引用计数，包装 ≠ 转移所有权**：`new HImage(obj)`/`new HXLDCont(obj)` 生成独立句柄，
   源对象与包装对象**都要释放**；只释放一边会漏（实测 500 次漏 160MB）。这条同样适用于 `dump_window_image` 的产出。
3. **`disp_text` 的白底框要 `'box','true'`**：只给 `'box_color'` 不会画框——`box` 是独立开关。
4. **`intensity` 算的是均值+偏差，不是 min/max**：取极值要用 `min_max_gray`（本次诊断时踩过，一度误判标注没画上）。
5. **算子有参数下限**：`measure_*` 的 `Sigma` 不能为 0（抛 #1302），夹取下限设 0.4；这类"算子级约束"必须在
   归一化里体现，否则非法配置会炸流程。
6. **越界要靠自己提前报中文错**：算子对"搜索区在图外"抛的是 #3022 英文错误，对操作员没有指导意义；
   提前用"哪个区、中心在哪、图多大"说清楚，才算满足"非法值不能炸 + 中文说明"。
7. **计量工具宁可报错也不要用"部分平均"**：检到 3/5 把卡尺就平均出一个数是"错误但不出错"的最危险失败模式。

---

# 第二批（同日交付）：点线距 / 圆直径 / 角度 三种拟合类测量

## 二.1 需求与交付范围

在第一批骨架上补齐三种拟合类测量类型，规格已逐条确认：

| 类型 | 搜索区 | 算法链 |
| --- | --- | --- |
| **点到线距** | 2 个旋转矩形（区1 出"点"、区2 拟合"线"） | 区1 各卡尺边点平均成代表点；区2 N 点 → `fit_line_contour_xld` → `distance_pl` 垂距 |
| **圆直径** | 1 个**圆形**搜索区（画布可拖）+ **环宽**参数 | 沿圆周**径向**均布 N 把卡尺 → `measure_pos` 取边点 → `fit_circle_contour_xld` → 直径 = 2×半径 |
| **角度** | 2 个旋转矩形（各拟合一条线） | 各自 `fit_line_contour_xld` → 求交点 → `angle_ll` → 归一化到 0~180°（度） |

输出端口仍为 6 个（名字/类型严格不变）。

## 二.2 关键取舍（全部经真实 HALCON 探针实证）

### 二.2.1 phi 方向约定的实证裁决（本批最重要的发现）

用"非对称位置探针"裁决（`GenRectangle2(250,250,π/6,80,3)` paint 后查四个角点灰度）：
长轴只经过 **(210,319)** 与 **(290,181)** → 本 HALCON 版本 `GenRectangle2`（画布/显示/paint）与
`gen_measure_rectangle2` 的 phi 约定**一致**：长轴单位向量 = **(−sinφ, cosφ)**（row, col）。

- 结论：**画布形状参数与测量算子参数直通**（第一批基石）在旋转情形下依然成立，`phi` 无需取负。
  （中间曾按相反假设改过取负，随即被裁决探针推翻并撤销——两种约定在 φ=0 等价，所以第一批全部
  φ=0 的实测没有暴露差异。）
- **连带修正第一批遗留**：`DistributeCalipers` 的卡尺排布向量原为 `(cosφ, −sinφ)`，与实际长轴
  `(−sinφ, cosφ)` 的点积 = −2sinφcosφ ≠ 0——**φ≠0 时排布方向不垂直于扫描方向**（第一批实测全在
  φ=0，未暴露）。已改为 `(cosφ, +sinφ)`。φ=0 行为不变，第一批回归实测 41.0/220.0 未受影响。

### 二.2.2 圆周径向均布（与"边缘垂直于 L1 轴"语义自洽）

圆的边缘（圆周）垂直于半径方向 → 每把卡尺的扫描方向取**径向**。圆周上角 a 处的点 =
`(R + r·sin a, C + r·cos a)`，其径向单位向量 = `(sin a, cos a)`；令 `(−sinφ, cosφ) = (sin a, cos a)`
得 **φ = −a**。卡尺中心放在用户画的圆周上、沿径向 ±环宽/2 扫描——圆心/半径摆不准时真实圆边
落在环内也能扫到（"环宽"参数存在的意义）。卡尺窗口半宽默认 3px（灰度平均窗口通用起点），
间距更小时自动夹到间距一半，避免相邻窗口互相污染。

### 二.2.3 角度的 0~180° 归一化（`angle_ll` 的正确打开方式）

`angle_ll` 返回有向角（−180°,180°]，**取值依赖两条线的方向向量取向**——fit 输出端点顺序决定，
直接取值可能把 150° 的楔角报成 −30°。做法：先解两无限直线的交点（行列式法，|sin夹角|<0.1° 判平行
报错），把两条线都归一化为"**从交点指向各自点分布远端**"的射线，再喂 `angle_ll`——射线取向固定后
`|angle|` 就是两射线间 0~180° 的真实夹角，无补角歧义。实测 150° 楔报 **150.1°**（非 -30/30）。
标注图在交点处画一段 ≤180° 的夹角弧（`gen_circle_contour_xld`，φ = atan2(Δrow, Δcol)）。

### 二.2.4 FittedGeometry 端口语义变化（记录在案）

- **拟合类**（点线距/圆直径/角度）：承载真正的拟合几何——拟合直线 / 拟合整圆（`gen_circle_contour_xld`，
  resolution 取半径的 1% ≈ 600 点）/ **两条拟合线**（角度，2 个轮廓对象 concat）；
- **非拟合类**（宽度/两点距）：保持第一批语义 = 测量连线。
- 标注图新增**品红"拟合几何"图层**（`AnnotationRenderer.Render` 加 `fittedGeometry` 参数），与青色
  "测量连线"分层：点线距画垂线段+垂足、角度画夹角弧、圆直径画直径线。

### 二.2.5 角度不受像素当量影响（易错点，代码与摘要都写明）

角度是量纲无关量（单位：度）。`MeasureValueMm = Kind==Angle ? MeasureValuePx : MeasureValuePx × 当量`，
信息栏/日志摘要也单独走角度分支（"角度=XX°（角度不受像素当量影响）"）。实测当量 0.5 时
点线距 98.25（=196.5×0.5）、圆 99.958（=199.917×0.5）、**角度仍 29.91°**（不变）✓。

### 二.2.6 拟合参数与失败路径（全部中文、不崩）

| 参数 | 默认 | 依据 |
| --- | --- | --- |
| 拟合方式 | **Tukey** | 卡尺偶尔扫到毛刺产生离群点，最小二乘会被一个坏点拽歪整条线/圆 |
| 最大残差 (px) | 1.0 | 亚像素边缘点正常应贴着特征边，主体点残差 >1px 说明混入杂边或特征弯曲。**口径 = 中位数**：用最大值检查会把 Tukey 已正确拟合的结果判死（一个毛刺点就超标），鲁棒拟合失去意义；中位数与 Tukey 容错哲学一致 |
| 最少点数 | 3 | 直线 2 点定线、圆 3 点定圆的数学下限，取 3 兼顾两者 |
| 环宽 (px) | 30 | 手画圆的半径/圆心误差通常几个~十几个像素，±15px 容差够用 |

失败路径（实测原文见二.4）：
- **点数不足**：`FitMinPoints` 前置检查（HALCON 的英文报错对操作员无指导意义）；
- **拟合算子异常**：catch `HOperatorException` 翻译成"为什么拟合不出来 + 怎么调"；
- **残差过大**：边缘点到拟合几何距离的**中位数** > FitMaxError；
- **圆退化（两条判据）**：①半径非有限/≤0/>图像对角线×10；②**几何判据**——拟合圆直径 < 点集最大
  跨度的 60%（共线/过扁点集的退化解），或点集跨度 <2px（边缘点几乎重合，特征可能是直线边）。
  实测教训：3 个共线点配 geotukey **不会报错**，而是收敛到 r≈31px 的"贴住少数点的小圆"，
  半径有限、残差中位数也达标——只有几何判据能拦住。

### 二.2.7 两个 HALCON .NET 兼容性实证（新版本签名/值集差异）

1. **`fit_circle_contour_xld` 的 Algorithm 值集是 geo 系列**（`geometric`/`geohuber`/`geotukey`），
   **不接受** fit_line 的 `regression`/`huber`/`tukey`——传错直接 #1301。按语义映射：
   Regression→`geometric`、Huber→`geohuber`、Tukey→`geotukey`。
   签名反射实测：`(contours, algorithm, maxNumPoints, maxClosureDist, clippingEndPoints, iterations,
   clippingFactor)`——**没有 PointOrder 输入**（那是输出），与 fit_line 的 `(contours, algorithm,
   maxNumPoints, clippingEndPoints, iterations, clippingFactor)` 不同构，不能想当然套用。
2. **`gen_circle_contour_xld` 多一个 `resolution` 输入**（轮廓点间距 px），取 `radius/100` 夹 [0.05,5]，
   整圆约 600 点，渲染平滑且无性能负担。
3. **measure 空结果重试**：实测进程内"同一调用首跑可能返回空、第二跑正常"（探针：同参连跑 3 遍 =
   0/1/1 条边）。对策：`MeasurePosWithRetry`/`MeasurePairsWithRetry` 空结果时新建句柄重跑，最多 4 次。
   真正没边缘时第二次起仍空、仍判失败，语义不变；只兜底偶发首跑空结果的误报。

## 二.3 修改文件清单

| 文件 | 改动 |
| --- | --- |
| `CaliperEnums.cs` | `MeasureKind` 加 PointToLineDistance/CircleDiameter/Angle；新增 `FitAlgorithmKind`（Regression/Huber/Tukey） |
| `CaliperMeasurePlugin.cs` | 拟合参数 4 个 `[StepConfig]`；`RequiredRegionCountOf`（圆直径=1 区）、默认播种支持圆形；`MeasureKind` 切换通知 IsFitMeasure/IsCircleMeasure；`ExecuteMeasureCore` if/else 改 switch + 三个拟合分支；新增 `CollectRegionEdgePoints`/`CollectCircleEdgePoints`/`TryFitLine`/`TryFitCircle`/`CreateMeasureHandle`/`MeasurePosWithRetry`/`MeasurePairsWithRetry`/`Median`/`Dist`；两点距重构为共用 `CollectRegionEdgePoints`（取值行为与第一批 bit 级一致）；`BuildSummary` 按类型给单位（角度=°）；`NormalizedParameters` 按类型校验形状（圆直径要求 Circle）+ 拟合参数夹取 |
| `AnnotationRenderer.cs` | `Render` 增加 `fittedGeometry` 参数（品红 2px 图层，空集不画；宽度/两点距传 null 保持第一批视觉不变） |
| `CaliperMeasureView.xaml` | 新增「④ 拟合参数」卡片（`IsFitMeasure` 控制显隐、环宽行 `IsCircleMeasure` 控制）；原「④ 判定与标定」改「⑤」；测量类型/重置按钮 ToolTip 更新 |

## 二.4 验证结果（真实输出）

验收方式与第一批相同：仓库外临时控制台（`C:\Temp\CaliperVerify2`，net9.0-windows + WPF，
引用 `Modules\Plugin.CaliperMeasure.dll`）合成图真跑，**验证后整目录删除、仓库内无残留**。
断言 **26 通过 / 0 失败**。关键输出（探针 0x 段为实证过程，主断言为最终结果）：

```
[PORTS] outputs=MeasureValue,Deviation,IsOk,MeasureImage,FittedGeometry,EdgePoints,Success,ErrorMessage
  [√] 测量类型 5 种  枚举成员=5（Width/PointToPoint/PointToLineDistance/CircleDiameter/Angle）
  [√] 默认拟合方式 Tukey / 最大残差 1.0 / 最少点数 3 / 环宽 30
---- 2. 点到线距（真值 196.5 px）----
  [√] 点到线距 = 196.5±0.8  实测=196.5          （水平线上边缘-下缘扫描语义，亚像素精确）
  [√] FittedGeometry=拟合线(1 条轮廓)  count=1
---- 3. 圆直径（真值 200 px）----
  [√] 圆直径 = 200±1.0  实测=199.917            （16 把径向卡尺 + geotukey，误差 0.083px）
  [√] FittedGeometry=拟合圆(1 条轮廓)  count=1
---- 4. 角度 ----
  [√] 夹角 30°±1.5  实测=29.91°
  [√] FittedGeometry=两条拟合线  count=2
  [√] 夹角 90°±1.5  实测=90°
  [√] 夹角 150°±1.5（楔形钝角，不得报 30/-30）  实测=150.1°   ← 归一化正确
---- 5. 像素当量 ----
  [√] 点线距当量 0.5 → 98.25±0.5  实测=98.25     （=196.5×0.5）
  [√] 圆直径当量 0.5 → 100±0.5  实测=99.958
  [√] 角度当量 0.5 → 仍 30°（不受当量影响）  实测=29.91°
---- 6. tukey vs regression（毛刺离群点，离群 12px）----
    tukey=196.5  regression=191.092（真值 196.5）
  [√] tukey 不被毛刺拽歪   [√] regression 被拽歪 5.4px
---- 7. 拟合失败路径（不崩 + 中文）----
  [√] 点数不足    Err='搜索区「线区」只收集到 2 个边缘点，少于最少点数 3：请增大卡尺数，或降低「最少点数」'
  [√] 圆退化      Err='拟合圆退化：3 个边缘点几乎重合（跨度仅 1.15 px，特征可能是直线边缘而非圆周）——…'
  [√] 残差过大    Err='搜索区「线区」拟合残差过大（主体点残差 1.75 px > 允许 1 px）：边缘点可能混入毛刺/杂边，
                       或特征本身弯曲——请增大卡尺数、增大平滑 Sigma，或把搜索区挪到更直的边缘上'
  [√] 圆点数不足  Err='拟合圆至少需要 3 个边缘点（当前 2 个）：请增大卡尺数'
---- 8. 资源不泄漏 ----
[MEM] 第 1~4 批(各 10 轮)：本批增量=0KB / 0KB / 0KB / 0KB   （5 类型混合 40 轮，无逐轮线性泄漏）
---- 9. 第一批回归 ----
  [√] 宽度亮带 → 41.0±0.3  实测=41              （paint 闭区间光栅化外扩 0.5px，与第一批口径一致）
  [√] 两点距 220px → 220.0  实测=220.0
---- 10. 视图 XAML 加载 ----
  [√] CaliperMeasureView 加载+布局+渲染  非空像素=564708
```

**编译与冒烟**：
- `dotnet build Plugins\Plugin.CaliperMeasure\Plugin.CaliperMeasure.csproj -v m -clp:ErrorsOnly` → 0 错误
  （完整依赖与 `--no-dependencies` 均 0 错误；本插件源文件 0 警告，解决方案级 762 个警告全部来自
  并行任务的依赖工程，与本插件无关）。
- 冒烟回归 `FlowCanvasChecks --no-build`：**通过 641 / 失败 14**（通过数较第一批 610 上升，为并行任务
  新增检查；14 条失败逐条核对全部属于相机插件与线序检测线，与任务书基线一致；搜索
  `Caliper|卡尺` 零匹配，与本插件无关）。
- 产物投递：`Modules\Plugin.CaliperMeasure.dll` 由 `Directory.Build.targets` 自动更新
  （注意：`--no-dependencies` 构建**不会**触发投递，改插件后必须完整构建一次再验证宿主侧）。

**探针实证过程中的弯路（诚实记录）**：本批先后用 8 组探针定位了 ①measure 空结果的时序现象、
②fit_circle 的值集与签名差异、③phi 约定（先误判需取负、后用非对称位置探针裁决撤销）、
④DistributeCalipers 的 L2 垂直方向错误、⑤圆退化的"重合点"形态。中间态的测试断言随实证同步修正，
最终全部收敛。

## 二.5 已知边界

1. **拟合类测量对边缘质量依赖高**：Tukey 只能容忍少量离群点（毛刺），主体边缘必须干净连续；
   残差检查用中位数口径，**多数点同时散掉**（特征弯曲/杂边大范围混入）会触发"残差过大"失败。
2. **圆搜索区的半径与环宽要摆正**：卡尺中心在圆周上、沿径向 ±环宽/2 扫描——圆半径画错超过环宽一半
   会"真实圆边在环外"而报未检出边缘；卡尺中心出图会被提前拦截（中文报错）。
3. **角度不受像素当量影响**（度是量纲无关量）；角度测量的两条线**接近平行**（<0.1°）时报错而不是硬算。
4. **角度的两条线按无限直线求交**：两特征线段不相交（延长线相交）时仍能测角，楔形夹角按"交点指向
   点分布远端"的两射线定义，0~180°。
5. **拟合点集来自 measure_pos 首条边**：Select='全部' 时每把取第一条（与两点距同口径）；特征上有多条
   平行边时（如亮带两沿）要注意搜索区只压住目标那条。
6. **phi 约定是本 HALCON 版本实测行为**（长轴 = (−sinφ, cosφ)）：换 HALCON 版本若约定变化，
   圆周径向卡尺与旋转矩形扫描会整体错位——升级 HALCON 后需用本批探针思路复测。
7. **measure 空结果重试（最多 4 次）是对 HALCON 偶发首跑空结果的兜底**：若现场出现"明明有边却报
   未检出"，优先检查图像通道/尺寸，其次考虑该现象并复测。
8. **`--no-dependencies` 构建不投递 Modules**：改完插件只做单工程快速编译时，宿主与外部验证加载的
   仍是旧 DLL——必须完整构建一次（带依赖）再验证。
9. **第一批遗留修正**：`DistributeCalipers` 的 L2 排布向量已改为 `(cosφ, sinφ)`——φ=0 行为不变
   （第一批两种类型全部实测复现），但**旋转矩形**的多卡尺排布从本批起才真正垂直于扫描方向。
10. **未纳入自动冒烟**（延续第一批边界）：FlowCanvasChecks 不覆盖本插件；本批 26 项断言的验证控制台
    已删除不可复现，建议后续把「196.5 点线距 / 200 圆直径 / 30-90-150 角度」固化成正式断言。
