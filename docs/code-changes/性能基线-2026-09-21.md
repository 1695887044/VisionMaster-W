# SCADA 性能基线留档（由 `baseline.ps1` 自动生成）

生成时间：2026-09-21 03:57:59

运行命令：`.\baseline.ps1`（Release；内部：`dotnet build` 后直接执行 `ScadaChecks.dll --stress`）

断言结果：通过: 1327  失败: 0

> 本文件**同一天重跑会覆盖**（同日多次跑通常是改代码试数字，跨天才有对比价值）。
> 数字**没有阈值**：断言只拦数量级退化，趋势必须人工和上一天这份文件对比。

## 留档数字

```
  ┌─ 基线数字（留档：粘贴进 docs/code-changes）────────────────────
  │ 图元数 | 20000 个 | 构造 48ms = 2.4µs/图元 | 查找 200000 次 8ms = 0.04µs/次
  │ 绑定数 | 20000 条（10000 图元 × 2）| 级联 4ms = 0.20µs/绑定 | 命中 10000
  │ 切页次数 | 5000 次（8 页 × 200 图元）| 27ms = 5.40µs/次 | 页栈峰值 32、Stop 后归零
  │ 报警数 | 500 条 × 10 轮 | 循环 12ms = 2.40µs/报警轮 | 历史 2000/2000 | 退订残留 0
  └────────────────────────────────────────────────────────────────
  ┌─ 长稳数字（留档：粘贴进 docs/code-changes）────────────────────
  │ 装配 | 1920×1080 | 200 图元 / 200 绑定 / 200 变量 | 建表命中 200、未命中 0、订阅 200
  │ 节拍 | 目标 20ms × 60s = 3000 拍 | 实跑 3000 拍 = 20.00ms/拍
  │ UI  | 最长停顿 47ms
  │ 内存 | 中途 28566KB → 终了 28404KB（+-162KB）
  │ 订阅 | 建表 200 → 中途 200 → 停止 0
  └────────────────────────────────────────────────────────────────
```

## 怎么读这两块

- **基线数字（`[BL]`）** = 瞬时单位成本：µs/图元、µs/绑定、µs/切页、µs/报警轮。看"快不快"。
- **长稳数字（`[ST]`）** = 持续负载下的曲线：节拍、UI 最长停顿、内存、订阅数。看"跑得住"。
- 订阅数**终了必须归零**（不归零 = 泄漏，这条有断言）；其余看趋势。
