<UserControl
    x:Class="UI.CustomControl.OverlayHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:UI.CustomControl"
    Grid.RowSpan="999"
    Grid.ColumnSpan="999"
    Panel.ZIndex="1000"
    IsHitTestVisible="True">

    <Grid>
        <ItemsControl
            Margin="0,20,20,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            ItemsSource="{Binding Source={x:Static local:Notifier.Messages}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        x:Name="NotificationBorder"
                        MinWidth="280"
                        MaxWidth="420"
                        Margin="0,0,0,12"
                        Padding="16,12"
                        Background="#FFFFFF"
                        BorderBrush="#E5E5E5"
                        BorderThickness="1"
                        CornerRadius="8"
                        MouseEnter="Notification_MouseEnter"
                        MouseLeave="Notification_MouseLeave">

                        <Border.Effect>
                            <DropShadowEffect
                                BlurRadius="25"
                                Direction="270"
                                Opacity="0.1"
                                ShadowDepth="5"
                                Color="Black" />
                        </Border.Effect>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Path
                                Grid.Column="0"
                                Width="18"
                                Height="18"
                                Margin="0,2,12,0"
                                VerticalAlignment="Top"
                                Data="{Binding IconPath}"
                                Fill="{Binding IconColor}"
                                Stretch="Uniform" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock
                                    FontSize="14"
                                    Foreground="#333333"
                                    Text="{Binding Message}"
                                    TextWrapping="Wrap" />

                                <TextBlock
                                    Margin="0,4,0,0"
                                    HorizontalAlignment="Right"
                                    FontSize="11"
                                    Foreground="#999999"
                                    Text="{Binding Time}" />
                            </StackPanel>
                        </Grid>

                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="Opacity"
                                            From="0"
                                            To="1"
                                            Duration="0:0:0.3" />
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                            From="120"
                                            To="0"
                                            Duration="0:0:0.5">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>

                        <Border.RenderTransform>
                            <TranslateTransform X="0" />
                        </Border.RenderTransform>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Grid
            x:Name="DialogRoot"
            Panel.ZIndex="1"
            Visibility="Collapsed">
            <Border
                x:Name="ModalMask"
                Background="#66000000"
                IsHitTestVisible="True"
                Visibility="Visible" />
            <Border
                x:Name="Backdrop"
                Background="#44000000"
                MouseLeftButtonDown="Backdrop_MouseLeftButtonDown">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Visibility, ElementName=DialogRoot}" Value="Visible">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="Opacity"
                                                From="0"
                                                To="1"
                                                Duration="0:0:0.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <Border
                x:Name="DialogCard"
                MinWidth="320"
                MaxWidth="1000"
                MaxHeight="800"
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="White"
                CornerRadius="8"
                RenderTransformOrigin="0.5,0.5">

                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="30"
                        Opacity="0.15"
                        ShadowDepth="0"
                        Color="Black" />
                </Border.Effect>

                <Border.RenderTransform>
                    <ScaleTransform ScaleX="0.95" ScaleY="0.95" />
                </Border.RenderTransform>

                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Visibility, ElementName=DialogRoot}" Value="Visible">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="Opacity"
                                                From="0"
                                                To="1"
                                                Duration="0:0:0.2" />
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                From="0.9"
                                                To="1.0"
                                                Duration="0:0:0.4">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                From="0.9"
                                                To="1.0"
                                                Duration="0:0:0.4">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        x:Name="TxtTitle"
                        Margin="0,0,0,16"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Foreground="#1A1A1A" />

                    <Grid Grid.Row="1" Margin="0,0,0,24">

                        <TextBlock
                            x:Name="TxtMessage"
                            FontSize="14"
                            Foreground="#444444"
                            LineHeight="20"
                            TextWrapping="Wrap" />

                        <ContentControl
                            x:Name="CustomContentPresenter"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Visibility="Collapsed" />
                    </Grid>

                    <StackPanel
                        Grid.Row="2"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                        <Button
                            Margin="0,0,12,0"
                            Padding="20,6"
                            Background="Transparent"
                            BorderBrush="#DCDCDC"
                            Click="BtnCancel_Click"
                            Content="取消" />
                        <Button
                            Padding="20,6"
                            Background="{StaticResource AccentBrush}"
                            BorderThickness="0"
                            Click="BtnConfirm_Click"
                            Content="确定"
                            Foreground="White" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>