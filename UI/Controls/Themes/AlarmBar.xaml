<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:UI.CustomControl">

    <Style TargetType="{x:Type local:AlarmBar}">
        <Setter Property="Height" Value="42" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Background" Value="#FAFAFA" />
        <Setter Property="Foreground" Value="#202020" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:AlarmBar}">
                    <Border x:Name="MainBorder"
                            BorderBrush="#E5E5E5" 
                            BorderThickness="0,1,0,1"
                            SnapsToDevicePixels="True">

                        <Border.Background>
                            <SolidColorBrush x:Name="MainBg" Color="#FAFAFA"/>
                        </Border.Background>

                        <Grid>
                            <Border VerticalAlignment="Top" Height="1" Background="#80FFFFFF" />

                            <Grid x:Name="ContentGrid" Margin="16,0">

                                <Border x:Name="NormalGrid" Visibility="Collapsed">
                                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                        <Grid Margin="0,0,10,0" VerticalAlignment="Center">
                                            <Ellipse x:Name="lightGlow" Width="14" Height="14" Fill="#0F7B0F" Opacity="0.2">
                                                <Ellipse.Effect>
                                                    <BlurEffect Radius="4" />
                                                </Ellipse.Effect>
                                            </Ellipse>
                                            <Ellipse Width="8" Height="8" Fill="#0F7B0F" />
                                        </Grid>

                                        <TextBlock VerticalAlignment="Center"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="#0F7B0F"
                                                   Text="SYSTEM OPERATIONAL" />
                                    </StackPanel>
                                </Border>

                                <Canvas x:Name="PART_Canvas" Background="Transparent" Margin="0,4">
                                    <ItemsControl x:Name="PART_ItemsHost" ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Height="34" Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#FFFFFF" 
                                                        CornerRadius="6" 
                                                        Margin="0,0,40,0" 
                                                        Padding="12,0,16,0"
                                                        BorderBrush="#EAEAEA"
                                                        BorderThickness="1">
                                                    <Border.Effect>
                                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.05" Direction="270" Color="Black"/>
                                                    </Border.Effect>

                                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                                        <Path Width="14" Height="14" Margin="0,0,8,0"
                                                              Data="M13,14H11V10H13M13,18H11V16H13M1,21H23L12,2L1,21Z"
                                                              Fill="{Binding LevelColor}"
                                                              Stretch="Uniform" />
                                                        <TextBlock VerticalAlignment="Center"
                                                                   FontSize="13"
                                                                   FontWeight="SemiBold"
                                                                   Foreground="{Binding LevelColor}"
                                                                   Text="{Binding Message}" />
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>

                                        <ItemsControl.RenderTransform>
                                            <TranslateTransform x:Name="PART_Translate" />
                                        </ItemsControl.RenderTransform>
                                    </ItemsControl>
                                </Canvas>
                            </Grid>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasAlarms" Value="False">
                            <Setter TargetName="NormalGrid" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_Canvas" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="MainBg" Storyboard.TargetProperty="Color" To="#F0F0F0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="MainBg" Storyboard.TargetProperty="Color" To="#FAFAFA" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation AutoReverse="True"
                                                     Storyboard.TargetName="lightGlow"
                                                     Storyboard.TargetProperty="Opacity"
                                                     From="0.05" To="0.4"
                                                     Duration="0:0:1.5">
                                        <DoubleAnimation.EasingFunction>
                                            <SineEase EasingMode="EaseInOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>